pipeline {
    agent any

    stages {
        stage('compile') {
            steps {
                echo 'compile World'
            }
        }
        stage('test') {
            steps {
                echo 'test World'
            }
        }
        stage('package') {
            steps {
                echo 'package World'
            }
        }
    }
}
